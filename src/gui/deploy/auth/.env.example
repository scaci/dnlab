# Postgres auth DB — copy to `.env` and edit.
# The real `.env` file must be chmod 600 and NEVER committed.

POSTGRES_DB=dnlab_auth
POSTGRES_USER=dnlab_gui
POSTGRES_PASSWORD=CHANGEME_use_openssl_rand_base64_32
POSTGRES_PORT=5432
